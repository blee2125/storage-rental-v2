import React from "react";
import { connect } from "react-redux";

function Home(props) {

    return (
        <div>
            home
        </div>
    )
}

export default connect(null, null) (Home)